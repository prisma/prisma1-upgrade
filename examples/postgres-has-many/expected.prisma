model PostModel {
  id        String    @default(cuid()) @id
  user      String
  UserModel UserModel @relation(fields: [user], references: [id])
}

model UserModel {
  id        String      @default(cuid()) @id
  PostModel PostModel[]
}