model Profile {
  bio  String
  id   String @default(cuid()) @id
  User User
}

model User {
  email   String  @unique
  id      String  @default(cuid()) @id
  profile String? @unique
  Profile Profile @relation(fields: [profile], references: [id])
}