model Post {
  authorId String
  id       String @default(cuid()) @id
  User     User   @relation(fields: [authorId], references: [id])
}

model User {
  id   String @default(cuid()) @id
  Post Post[]
}