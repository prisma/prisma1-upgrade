model Post {
  id        String   @default(cuid()) @id
  updatedAt DateTime
  createdAt DateTime
  Tag       Tag[]    @relation(references: [id])
}

model Tag {
  id        String   @default(cuid()) @id
  updatedAt DateTime
  createdAt DateTime
  Post      Post[]   @relation(references: [id])
}