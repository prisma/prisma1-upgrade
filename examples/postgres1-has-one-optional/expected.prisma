model Profile {
  id        String   @default(cuid()) @id
  updatedAt DateTime
  createdAt DateTime
  userId    String?  @unique
  author    User?    @relation(fields: [userId], references: [id])
}

model User {
  id        String   @default(cuid()) @id
  updatedAt DateTime
  createdAt DateTime
  profile   Profile
}