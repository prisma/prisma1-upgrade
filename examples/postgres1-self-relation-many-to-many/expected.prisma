datasource db {
  provider = "postgresql"
  url      = "postgres://m@localhost:5432/prisma_test?schema=postgres1-self-relation-many-to-many%24dev"
}

model User {
  createdAt DateTime
  id        String   @default(cuid()) @id
  updatedAt DateTime
  User_A    User[]   @relation("UserInvitation", references: [id])
  User_B    User[]   @relation("UserInvitation", references: [id])
}