model Post {
  authorId  String
  createdAt DateTime
  id        String   @default(cuid()) @id
  updatedAt DateTime
  User      User     @relation(fields: [authorId], references: [id])
}

model User {
  createdAt DateTime
  id        String   @default(cuid()) @id
  updatedAt DateTime
  Post      Post[]
}