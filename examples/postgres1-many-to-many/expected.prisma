model Post {
  createdAt DateTime
  id        String   @default(cuid()) @id
  updatedAt DateTime
  Tag       Tag[]    @relation(references: [id])
}

model Tag {
  createdAt DateTime
  id        String   @default(cuid()) @id
  updatedAt DateTime
  Post      Post[]   @relation(references: [id])
}