model Post {
  id   String  @default(cuid()) @id
  meta Json
  user String?
  User User?   @relation(fields: [user], references: [id])
  @@index([user], name: "user")
}

model User {
  id   String @default(cuid()) @id
  Post Post[]
}