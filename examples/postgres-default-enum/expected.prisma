datasource db {
  provider = "postgresql"
  url      = "postgres://m@localhost:5432/prisma_test?schema=postgres-default-enum%24dev"
}

model User {
  id   String @default(cuid()) @id
  role Role   @default(CUSTOMER)
}

enum Role {
  ADMIN
  CUSTOMER
}